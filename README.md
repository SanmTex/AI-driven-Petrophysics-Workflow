# AI-driven-Petrophysics-Workflow

Notebook and CSVs for an ensemble regression petrophysics workflow.
Placeholders and instructions:
- Run Jupyter from the repo root.
- Data files are included in the repo root (or data/ if you move them).
